<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Level <%= level.level_number %> - JNV Quiz Admin</title>
    <%- include('partials/head') %>
</head>
<body>
    <%- include('partials/nav') %>

    <div class="container">
        <h1>Edit Level <%= level.level_number %></h1>

        <% if (message) { %>
            <div class="success"><%= message %></div>
        <% } %>
        <% if (error) { %>
            <div class="error"><%= error %></div>
        <% } %>

        <div class="card">
            <h2>Level Details</h2>

            <form method="POST" action="/admin/levels/<%= level.level_number %>/update">
                <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 20px;">
                    <div class="form-group">
                        <label for="level_number">Level Number</label>
                        <input type="number" id="level_number" value="<%= level.level_number %>"
                               disabled style="background: #f5f5f5;">
                        <small style="color: #666;">Level number cannot be changed</small>
                    </div>

                    <div class="form-group">
                        <label for="duration_seconds">Duration (seconds) *</label>
                        <input type="number" id="duration_seconds" name="duration_seconds"
                               value="<%= level.duration_seconds %>" min="60" max="3600" required>
                        <small style="color: #666;">Quiz duration in seconds</small>
                    </div>
                </div>

                <div class="form-group">
                    <label for="title">Title *</label>
                    <input type="text" id="title" name="title" value="<%= level.title %>" required maxlength="200">
                </div>

                <div class="form-group">
                    <label for="subtitle">Subtitle</label>
                    <input type="text" id="subtitle" name="subtitle" value="<%= level.subtitle || '' %>" maxlength="300">
                </div>

                <div class="form-group">
                    <label>
                        <input type="checkbox" name="is_active" <%= level.is_active ? 'checked' : '' %>>
                        Active (visible to users)
                    </label>
                </div>

                <div style="background: #f8f9fa; padding: 15px; border-radius: 5px; margin: 20px 0;">
                    <strong>Timestamps:</strong>
                    <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 15px; margin-top: 10px; color: #666; font-size: 14px;">
                        <div>Created: <%= new Date(level.created_at).toLocaleString('en-IN', { timeZone: 'Asia/Kolkata' }) %></div>
                        <div>Updated: <%= new Date(level.updated_at).toLocaleString('en-IN', { timeZone: 'Asia/Kolkata' }) %></div>
                    </div>
                </div>

                <div style="display: flex; gap: 10px;">
                    <button type="submit">Update Level</button>
                    <a href="/admin/levels" class="btn" style="background: #6c757d; text-decoration: none;">Cancel</a>
                </div>
            </form>
        </div>

        <div style="margin-top: 20px;">
            <a href="/admin/levels" style="color: #0066cc; text-decoration: none;">← Back to Levels</a>
        </div>
    </div>
</body>
</html>
