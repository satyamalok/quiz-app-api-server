<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit User - <%= user.name %> - JNV Quiz Admin</title>
    <%- include('partials/head') %>
</head>
<body>
    <%- include('partials/nav') %>

    <div class="container">
        <h1>Edit User: <%= user.name %></h1>

        <% if (message) { %>
            <div class="success"><%= message %></div>
        <% } %>
        <% if (error) { %>
            <div class="error"><%= error %></div>
        <% } %>

        <div class="card">
            <h2>User Information</h2>
            <p style="color: #666; margin-bottom: 20px;">
                Edit user details below. Be careful when modifying XP and level values as they affect user progress.
            </p>

            <!-- Profile Image Display -->
            <div style="display: flex; align-items: center; gap: 20px; margin-bottom: 25px; padding: 15px; background: #f8f9fa; border-radius: 8px;">
                <% if (user.profile_image_url) { %>
                    <img src="<%= user.profile_image_url %>" alt="Profile Picture"
                         style="width: 80px; height: 80px; border-radius: 50%; object-fit: cover; border: 3px solid #ddd;">
                <% } else { %>
                    <div style="width: 80px; height: 80px; border-radius: 50%; background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); display: flex; align-items: center; justify-content: center;">
                        <span style="font-size: 32px; color: white; font-weight: bold;"><%= user.name ? user.name.charAt(0).toUpperCase() : 'U' %></span>
                    </div>
                <% } %>
                <div>
                    <div style="font-weight: bold; font-size: 18px;"><%= user.name %></div>
                    <div style="color: #666; font-size: 14px;"><%= user.phone %></div>
                    <div style="color: #888; font-size: 12px; margin-top: 5px;">
                        <% if (user.profile_image_url) { %>
                            <span style="color: #28a745;">✓ Profile picture set</span>
                        <% } else { %>
                            <span style="color: #999;">No profile picture</span>
                        <% } %>
                    </div>
                </div>
            </div>

            <form method="POST" action="/admin/users/<%= user.phone %>/update">
                <!-- Basic Info -->
                <div class="form-group">
                    <label for="phone">Phone Number *</label>
                    <input type="text" id="phone" value="<%= user.phone %>" disabled style="background: #f5f5f5;">
                    <small style="color: #666;">Phone number cannot be changed</small>
                </div>

                <div class="form-group">
                    <label for="name">Full Name *</label>
                    <input type="text" id="name" name="name" value="<%= user.name %>" required>
                </div>

                <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 20px;">
                    <div class="form-group">
                        <label for="district">District</label>
                        <input type="text" id="district" name="district" value="<%= user.district || '' %>">
                    </div>

                    <div class="form-group">
                        <label for="state">State</label>
                        <input type="text" id="state" name="state" value="<%= user.state || '' %>">
                    </div>
                </div>

                <!-- Progress and Stats -->
                <div style="display: grid; grid-template-columns: 1fr 1fr 1fr; gap: 20px;">
                    <div class="form-group">
                        <label for="xp_total">Total XP *</label>
                        <input type="number" id="xp_total" name="xp_total" value="<%= user.xp_total %>" min="0" required>
                        <small style="color: #666;">Current: <%= user.xp_total %></small>
                    </div>

                    <div class="form-group">
                        <label for="current_level">Current Level *</label>
                        <input type="number" id="current_level" name="current_level" value="<%= user.current_level %>" min="1" max="100" required>
                        <small style="color: #666;">Must be between 1-100</small>
                    </div>

                    <div class="form-group">
                        <label for="total_ads_watched">Videos Watched *</label>
                        <input type="number" id="total_ads_watched" name="total_ads_watched" value="<%= user.total_ads_watched %>" min="0" required>
                        <small style="color: #666;">Current: <%= user.total_ads_watched %></small>
                    </div>
                </div>

                <!-- Read-only Info -->
                <div style="background: #f8f9fa; padding: 15px; border-radius: 5px; margin: 20px 0;">
                    <h3 style="margin-bottom: 10px; font-size: 16px;">Read-Only Information</h3>
                    <div style="display: grid; grid-template-columns: repeat(2, 1fr); gap: 15px; color: #666; font-size: 14px;">
                        <div>
                            <strong>Referral Code:</strong> <code><%= user.referral_code %></code>
                        </div>
                        <div>
                            <strong>Date Joined:</strong> <%= new Date(user.date_joined).toLocaleDateString() %>
                        </div>
                        <div>
                            <strong>Profile Image:</strong> <%= user.profile_image_url ? 'Set' : 'Not set' %>
                        </div>
                        <div>
                            <strong>Last Updated:</strong> <%= new Date(user.updated_at).toLocaleString() %>
                        </div>
                    </div>
                </div>

                <div style="display: flex; gap: 10px;">
                    <button type="submit">Update User</button>
                    <a href="/admin/users/<%= user.phone %>/view" class="btn" style="background: #6c757d; text-decoration: none;">Cancel</a>
                </div>
            </form>
        </div>

        <div class="card" style="background: #fff3cd; border-left: 4px solid #ffc107;">
            <h3 style="margin-bottom: 10px;">⚠️ Important Notes</h3>
            <ul style="margin: 10px 0 0 20px; color: #666;">
                <li>Changing XP or level values will not affect the user's level attempt history</li>
                <li>Use this feature carefully as it directly modifies user progress</li>
                <li>Referral code and date joined cannot be changed</li>
                <li>Profile images can only be updated by the user through the mobile app</li>
            </ul>
        </div>

        <!-- Back Links -->
        <div style="margin-top: 20px; display: flex; gap: 20px;">
            <a href="/admin/users/<%= user.phone %>/view" style="color: #0066cc; text-decoration: none;">← Back to Profile</a>
            <a href="/admin/users/list" style="color: #0066cc; text-decoration: none;">← Back to User List</a>
        </div>
    </div>
</body>
</html>
